# Gym Manager Architecture — Canonical Document

This file is retained only as a compatibility pointer for older references.

The single source of truth for architecture, security boundaries, V1 scope and release gates is:

`docs/ARCHITECTURE.md`

Do not add new architecture rules here. Update `docs/ARCHITECTURE.md` instead.
